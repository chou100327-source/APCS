# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 這是什麼專案

一份 **APCS 初級・中級 Python 自學教材 + 互動學習網站**。使用者本人是學習者，正在用 Python 準備 APCS 檢定。產出物是繁體中文 Markdown 講義、Python 練習題解，以及 `website/` 這個純前端學習站。

原始需求與規格來自 `APCS講義.docx`（等同 README，動手前務必先讀；裡面有 18 單元全文、11 題實作題、50 天計畫、配色與技術規格）。

- **語言**：教材與說明用繁體中文（台灣），所有程式碼用 **Python 3**
- **考試日期**：2026-11-01；計畫期間 2026-09-13 起共 50 天，每天約 30 分鐘
- **目標範圍**：**只排初級與中級題本**（觀念＋實作）。進階延伸（函式、遞迴、堆疊佇列、排序、搜尋、複雜度）使用者說時間不夠，**計畫不排入**；教材保留在側邊欄最下面當選讀
- **讀書節奏**：**兩天一組**——第 1 天讀單元（觀念）、第 2 天做該單元的練習頁或實作題。12 個單元排在 9/13–10/6，之後 10/7–10/31 一樣兩天一組做總測、剩下的初中級實作、考古題與錯題重練
- **讀者假設**：先前學過 Python 但已生疏，需要先喚回語法再進入演算法。觀念題與實作題兩者並重
- 單元編排依 APCS 官方公布的「評量架構」

## 目錄結構

```
01-初級/          7 單元：輸入輸出、資料型態、算術、邏輯、位元、條件、迴圈
02-中級/          5 單元：一維陣列、二維陣列、字元與編碼、字串、文字處理與流程模擬
03-進階延伸/      6 單元：函式、遞迴、堆疊佇列、排序、搜尋、時間複雜度
練習/             初級・中級的「練習日」練習頁（01 輸出格式、02 型別轉換、04 條件式、08 串列、10 凱撒）
實作題/           11 題實作練習（題目 + 提示，參考解答放檔案最下方）
考前檢查表.md
APCS講義.docx     原始規格書（內容來源，勿刪）
website/          互動學習網站（純前端）
api/sync.js       跨裝置同步的 Vercel Serverless Function（內容無關，改教材時不用動）
```

## 產出教材時的固定規格

**每一節講義**依序包含：

1. **觀念講解** — 白話，講清楚「什麼時候該用這個」
2. **Python 程式碼範例** — 可直接套用、有註解
3. **典型考題／常見陷阱** — 用引言區塊（`>`）標出最容易踩的坑
4. **五分鐘小練習** — 用 ` ```quiz ` 區塊寫 3~4 題互動選擇題（語法見下）
5. **常見錯誤** — 這個主題最容易出錯的地方

內嵌小測驗的語法（網站會自動轉成可點選的互動題）：

````
```quiz
Q: 問題文字（可用反引號寫行內程式）
* 正確選項
- 錯誤選項
- 錯誤選項
E: 答完後顯示的解說（可加 [[viz:KEY]] 掛動畫）
```
````

教材內文也可以單獨一行寫 `[[viz:KEY]]` 把互動動畫嵌在該位置，可用的 KEY 見 `website/viz.js` 最下方的 `NAMES`。

## Python 撰碼慣例（教材所有範例都要遵守）

- Python 3 語法；縮排一律 **4 個空白**，不要混用 Tab
- 讀輸入用 `int(input())`、`map(int, input().split())`、`[list(map(int, input().split())) for _ in range(r)]`
- **只印答案本身**，不要印「請輸入…」之類的提示文字（判題逐字比對）
- 二維陣列一律 `[[0] * c for _ in range(r)]`，**絕不**用 `[[0]*c]*r`
- 變數名避開內建名稱（`list`、`str`、`sum`、`input`、`max`、`min`、`id`…）
- 教材中要主動提醒的地雷：`-7 // 2 = -4`、`-7 % 2 = 1`、`**` 右結合、`int()` 是捨去不是四捨五入、`round()` 的銀行家捨入、`a = a.sort()` 會變 `None`、字串不可變

## 網站架構（website/）

純前端、無建置步驟。要開網站得用本地伺服器（`python3 -m http.server 8000`，開 `http://localhost:8000/website/`），因為要 fetch 上層資料夾的 `.md`。

| 檔案 | 作用 |
|---|---|
| `index.html` | 骨架與六個分頁 |
| `plan.js` | **單一資料來源**：50 天計畫 `STUDY`（`weeks` 週次 + `days` 逐日，用日期對應；每天用 `p` 開教材或 `go` 跳分頁）、`LESSON_VIZ`、日期工具 |
| `explain.js` | 逐行走讀的說明產生器：每行給總結、拆解說明、所屬區塊（`EXPLAIN.line(lines, i)`） |
| `app.js` | 主邏輯：儀表板、教材閱讀器、觀念題、Pyodide 判題、筆記、跨裝置同步 |
| `quiz.js` | 觀念題題庫（`w` = 初級／中級／進階延伸，`a` = 正解索引） |
| `problems.js` | 判題題目與測資（`{id, title, statement, tests:[{in,out}]}`） |
| `viz.js` | 10 組互動動畫，對外提供 `window.VIZ` |
| `cheat.js` | 考前速查表 |
| `icons.js` / `style.css` / `sync.js` | 線條 icon、樣式、同步用戶端 |

要點：

- **判題用 Pyodide**（`pyodide@314.0.6`，CDN 載入），Python 直接在瀏覽器裡跑，程式碼不會送到任何伺服器。第一次要下載約 10MB，需要網路；教材、計畫、觀念題離線可用。
- `input()` 被換成從測資逐行取值；用 `sys.settrace` 計行數當作 TLE 上限，防止無窮迴圈卡死分頁。
- 觀念題**每次隨機抽 15 題並打亂選項**（題庫裡正解一律寫在第一個選項，靠 `shuffleOptions` 打亂）。預設範圍是「初級＋中級」（值 `core`）；「全部」會含進階延伸，放在選單後面。
- 首頁進度環統計的是 50 天計畫的每一天（key 是 `day:日期`）；教材「讀完了」的標記用教材路徑當 key，兩者不會互相干擾。
- 介面不使用 emoji，一律線條 icon；教材內文的裝飾性 emoji 顯示時會自動換成 icon。
- 配色依講義規格：紙白 `#FDFDFA`、墨藍 `#1B2333`、藍筆 `#2B5CB8`、紫 `#7A4FB5`、綠 `#2E7D5B`、訂正紅 `#C8322D`、螢光黃 `#FFE86B`；字體 Noto Sans TC + JetBrains Mono；圓角 8 / 12 / 18。

## 動教材或題目時要記得

- 新增／搬動單元檔案 → 同步更新 `plan.js` 的 `STUDY.days`（裡面的 `p`）與 `app.js` 的 `NAV_SECTIONS`
- 計畫從 9/13（週日）起算，剛好切成 7 個完整週（週日→週六）＋考試日，共 8 週 50 天
- **每一天都要點得進去**：有頁面就填 `p`；實作題日用 `go: "judge:題目id"`、測驗日用 `go: "quiz:範圍"`、考古題用 `go: "url:…"`（格式見 plan.js 開頭註解）
- 練習頁的格式：今日任務 → 題目（先猜再驗證）→ 小程式 → ```quiz → 解答放最下方；答案一律先用 Python 實跑確認
- 教材裡要連到別篇就寫相對於該 .md 的路徑（例如 `../01-初級/01-輸入與輸出.md`），網站會自動在站內打開
- 新增判題題目 → `problems.js` 加一筆，**測資一定要用參考解答實際跑過驗證**（自己手算很容易錯）
- 進度、筆記、程式碼存在瀏覽器 `localStorage`，並透過 `api/sync.js` 跨裝置同步

## 工具分工

- 寫題、debug：網站的「程式判題」分頁（Pyodide，直接在瀏覽器跑測資）
- 累積題數、考古題：[ZeroJudge](https://zerojudge.tw/) 的 APCS 專區
- 歷屆試題：[APCS 官方網站](https://apcs.csie.ntnu.edu.tw/)
