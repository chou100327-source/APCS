# 考前檢查表

考前一週開始，每天花 5 分鐘掃一遍。任何一項答不出來，就回去翻對應單元。

---

## 語法基礎

- [ ] 能不查資料寫出 `int(input())`、`map(int, input().split())` 讀取輸入
- [ ] 清楚 `-7 // 2 = -4`、`-7 % 2 = 1`，知道與 C/Java 的差異
- [ ] 記得 `**` 是由右往左結合，`2 ** 3 ** 2 = 512`
- [ ] 知道 `int(7.9)` 是 7、`int(-7.9)` 是 -7（捨去不是四捨五入）
- [ ] 知道 `/` 一定回傳浮點數，要整數除法用 `//`

## 位元運算

- [ ] 能手算 `&`、`|`、`^` 並知道左移右移等於乘除 2 的次方
- [ ] 會用 `n & 1` 判奇偶、XOR 找出落單的數字
- [ ] 會用 `1 << k` 做遮罩，並用 `|=`、`&= ~mask`、`^=` 開關切換

## 陣列與二維陣列

- [ ] 用 `[[0]*c for _ in range(r)]` 建二維陣列，**不用** `[[0]*c]*r`
- [ ] 寫得出方向陣列 `dr`/`dc` 並記得做邊界檢查 `0 <= ni < R and 0 <= nj < C`
- [ ] 記得 `sorted(a)` 回傳新串列、`a.sort()` 原地排序回傳 None
- [ ] 切片 `a[start:stop:step]` 熟練，會用 `a[::-1]` 反轉

## 字元與字串

- [ ] 記得 `ord('A')=65`、`ord('a')=97`、`ord('0')=48`
- [ ] 會用 `% 26` 處理字母繞回，寫得出凱撒加解密
- [ ] 知道字串不可變，反轉要用 `s[::-1]`
- [ ] 會用長度 26 的陣列統計字母頻率

## 進階觀念（選讀：計畫沒有排入，時間不夠可以跳過）

- [ ] 說得出遞迴的兩個要素：終止條件、規模變小
- [ ] 分得清堆疊（LIFO）與佇列（FIFO），知道括號匹配要用堆疊
- [ ] 記得三種 O(n²) 排序的核心動作，會判斷「排到第幾輪」
- [ ] 二分搜尋的前提是資料已排序，手動模擬得出 lo/hi/mid 的變化
- [ ] 背得出 O(1) < O(log n) < O(n) < O(n log n) < O(n²) < O(2ⁿ)

## 解題流程

- [ ] 動手前先用範例資料在紙上跑一遍
- [ ] 送出前檢查邊界：只有 1 筆、空輸入、第一個與最後一個元素
- [ ] 確認輸出格式：換行、空白、小數位數是否符合題目要求
- [ ] 只印答案本身，不印任何提示文字

## 考場準備

- [ ] 確認考場位置與交通時間
- [ ] 備妥身分證件
- [ ] 考前一晚早睡，當天提前到場
- [ ] 進場後：觀念題先掃過一遍把會的寫完，實作題從第 1 題按順序寫

---

> **延伸練習資源**：歷屆試題可至 APCS 官方網站 [apcs.csie.ntnu.edu.tw](https://apcs.csie.ntnu.edu.tw/) 下載，線上評測可使用 [ZeroJudge](https://zerojudge.tw/) 的 APCS 專區。
